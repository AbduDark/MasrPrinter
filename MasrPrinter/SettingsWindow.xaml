<Window x:Class="MasrPrinter.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="الإعدادات"
        Height="700" Width="800"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft"
        Background="{StaticResource BackgroundGradient}"
        RenderOptions.BitmapScalingMode="LowQuality"
        TextOptions.TextFormattingMode="Display">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- رأس الصفحة -->
        <Border Grid.Row="0" Background="{StaticResource SuccessGradient}" Padding="32,24">
            <Border.Effect>
                <DropShadowEffect Color="#10B981" Opacity="0.4" BlurRadius="30" ShadowDepth="8" Direction="270"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Viewbox Width="40" Height="40" Margin="0,0,16,0">
                    <Canvas Width="24" Height="24">
                        <Path Fill="White" Data="M12,15.5A3.5,3.5 0 0,1 8.5,12A3.5,3.5 0 0,1 12,8.5A3.5,3.5 0 0,1 15.5,12A3.5,3.5 0 0,1 12,15.5M19.43,12.97C19.47,12.65 19.5,12.33 19.5,12C19.5,11.67 19.47,11.34 19.43,11L21.54,9.37C21.73,9.22 21.78,8.95 21.66,8.73L19.66,5.27C19.54,5.05 19.27,4.96 19.05,5.05L16.56,6.05C16.04,5.66 15.5,5.32 14.87,5.07L14.5,2.42C14.46,2.18 14.25,2 14,2H10C9.75,2 9.54,2.18 9.5,2.42L9.13,5.07C8.5,5.32 7.96,5.66 7.44,6.05L4.95,5.05C4.73,4.96 4.46,5.05 4.34,5.27L2.34,8.73C2.21,8.95 2.27,9.22 2.46,9.37L4.57,11C4.53,11.34 4.5,11.67 4.5,12C4.5,12.33 4.53,12.65 4.57,12.97L2.46,14.63C2.27,14.78 2.21,15.05 2.34,15.27L4.34,18.73C4.46,18.95 4.73,19.03 4.95,18.95L7.44,17.94C7.96,18.34 8.5,18.68 9.13,18.93L9.5,21.58C9.54,21.82 9.75,22 10,22H14C14.25,22 14.46,21.82 14.5,21.58L14.87,18.93C15.5,18.67 16.04,18.34 16.56,17.94L19.05,18.95C19.27,19.03 19.54,18.95 19.66,18.73L21.66,15.27C21.78,15.05 21.73,14.78 21.54,14.63L19.43,12.97Z"/>
                    </Canvas>
                </Viewbox>
                <TextBlock Text="⚙️ إعدادات الطباعة"
                           FontSize="32"
                           FontWeight="Bold"
                           Foreground="White"
                           VerticalAlignment="Center">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.2" BlurRadius="8" ShadowDepth="2"/>
                    </TextBlock.Effect>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- المحتوى -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="32,24">

                <!-- مواصفات الورق -->
                <Border Style="{StaticResource CardPanel}" Margin="0,0,0,16">
                    <StackPanel>
                        <Border Background="{StaticResource PrimaryGradient}"
                                CornerRadius="14"
                                Padding="16,10"
                                Margin="0,0,0,20">
                            <TextBlock Text="📐 مواصفات الورق"
                                      FontSize="20"
                                      FontWeight="Bold"
                                      Foreground="White"/>
                        </Border>

                        <TextBlock Text="عرض الورق (مم):"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource SecondaryTextColor}"
                                   Margin="10,8,10,8"/>
                        <TextBox x:Name="PaperWidthTextBox"
                                 Text="80"
                                 FontSize="18"
                                 Padding="10,6"
                                 Margin="10,0,10,8"
                                 MinHeight="40"
                                 Background="White"
                                 Foreground="#222"
                                 BorderBrush="#DDD"
                                 BorderThickness="1"
                                 VerticalContentAlignment="Center"
                                 TextChanged="PaperSize_TextChanged"/>

                        <TextBlock Text="ارتفاع الورق (مم):"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource SecondaryTextColor}"
                                   Margin="10,16,10,8"/>
                        <TextBox x:Name="PaperHeightTextBox"
                                 Text="50"
                                 FontSize="18"
                                 Padding="10,6"
                                 Margin="10,0,10,8"
                                 MinHeight="40"
                                 Background="White"
                                 Foreground="#222"
                                 BorderBrush="#DDD"
                                 BorderThickness="1"
                                 VerticalContentAlignment="Center"
                                 TextChanged="PaperSize_TextChanged"/>
                    </StackPanel>
                </Border>

                <!-- إعدادات الطابعة -->
                <Border Style="{StaticResource CardPanel}" Margin="0,0,0,16">
                    <StackPanel>
                        <Border Background="{StaticResource AccentGradient}"
                                CornerRadius="14"
                                Padding="16,10"
                                Margin="0,0,0,20">
                            <TextBlock Text="🖨️ إعدادات الطابعة"
                                      FontSize="20"
                                      FontWeight="Bold"
                                      Foreground="White"/>
                        </Border>

                        <TextBlock Text="اختيار الطابعة:"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource SecondaryTextColor}"
                                   Margin="10,8,10,8"/>
                        <ComboBox x:Name="PrinterComboBox"
                                  FontSize="16"
                                  Height="44"
                                  Padding="8,4"
                                  Margin="10,0,10,8"
                                  Background="White"
                                  Foreground="#222"
                                  BorderBrush="#DDD"
                                  BorderThickness="1"/>

                        <TextBlock Text="مستوى الحرارة (0-100):"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource SecondaryTextColor}"
                                   Margin="10,20,10,8"/>
                        <Grid Margin="10,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>
                            <Slider x:Name="ThermalLevelSlider"
                                    Grid.Column="0"
                                    Minimum="0"
                                    Maximum="100"
                                    Value="50"
                                    TickFrequency="10"
                                    IsSnapToTickEnabled="True"
                                    VerticalAlignment="Center"
                                    Height="30"
                                    ValueChanged="Slider_ValueChanged"/>
                            <Border Grid.Column="1"
                                    Background="{StaticResource BackgroundColor}"
                                    CornerRadius="10"
                                    Padding="12,8"
                                    Margin="12,0,0,0">
                                <TextBlock Text="{Binding Value, ElementName=ThermalLevelSlider, StringFormat={}{0:0}}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           FontWeight="Bold"
                                           FontSize="18"
                                           Foreground="{StaticResource TextColor}"/>
                            </Border>
                        </Grid>

                        <TextBlock Text="جودة الباركود (DPI):"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource SecondaryTextColor}"
                                   Margin="10,20,10,8"/>
                        <Grid Margin="10,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>
                            <Slider x:Name="BarcodeQualitySlider"
                                    Grid.Column="0"
                                    Minimum="150"
                                    Maximum="600"
                                    Value="300"
                                    TickFrequency="150"
                                    IsSnapToTickEnabled="True"
                                    VerticalAlignment="Center"
                                    Height="30"
                                    ValueChanged="Slider_ValueChanged"/>
                            <Border Grid.Column="1"
                                    Background="{StaticResource BackgroundColor}"
                                    CornerRadius="10"
                                    Padding="12,8"
                                    Margin="12,0,0,0">
                                <TextBlock Text="{Binding Value, ElementName=BarcodeQualitySlider, StringFormat={}{0:0} DPI}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           FontWeight="Bold"
                                           FontSize="16"
                                           Foreground="{StaticResource TextColor}"/>
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- الرقم المخصص -->
                <Border Style="{StaticResource CardPanel}" Margin="0,0,0,16">
                    <StackPanel>
                        <Border Background="{StaticResource SuccessGradient}"
                                CornerRadius="14"
                                Padding="16,10"
                                Margin="0,0,0,20">
                            <TextBlock Text="🔢 الرقم المخصص"
                                      FontSize="20"
                                      FontWeight="Bold"
                                      Foreground="White"/>
                        </Border>

                        <TextBlock Text="الرقم الافتراضي للطباعة:"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource SecondaryTextColor}"
                                   Margin="10,8,10,8"/>
                        <TextBox x:Name="CustomNumberTextBox"
                                 Text="1"
                                 FontSize="18"
                                 Padding="10,6"
                                 Margin="10,0,10,8"
                                 MinHeight="40"
                                 Background="White"
                                 Foreground="#222"
                                 BorderBrush="#DDD"
                                 BorderThickness="1"
                                 VerticalContentAlignment="Center"/>

                        <Border Background="#DBEAFE"
                                CornerRadius="12"
                                Padding="16"
                                Margin="10,16,10,0"
                                BorderBrush="{StaticResource InfoColor}"
                                BorderThickness="1">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="💡" FontSize="18" Margin="0,0,10,0"/>
                                <TextBlock Text="هذا الرقم سيستخدم كقيمة افتراضية في نافذة الطباعة المخصصة"
                                           Foreground="{StaticResource InfoColor}"
                                           FontSize="13"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- معاينة الحجم -->
                <Border x:Name="PreviewSizeBox"
                        Style="{StaticResource CardPanel}"
                        Margin="0,0,0,16"
                        Background="#DBEAFE"
                        BorderBrush="{StaticResource InfoColor}">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="📏 معاينة الحجم"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   Foreground="{StaticResource InfoColor}"
                                   Margin="0,0,0,16"/>
                        <Border x:Name="SizePreviewBorder"
                                BorderBrush="{StaticResource InfoColor}"
                                BorderThickness="3"
                                Background="White">
                            <TextBlock Text="حجم الملصق"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="{StaticResource SecondaryTextColor}"
                                       Padding="20"/>
                        </Border>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- الأزرار -->
        <Border Grid.Row="2" Background="White" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,1,0,0" Padding="32,20">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="20" ShadowDepth="0" Direction="90"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="💾 حفظ الإعدادات"
                        Style="{StaticResource SuccessButton}"
                        Click="SaveButton_Click"
                        Height="56"
                        FontSize="18"/>

                <Button Grid.Column="2"
                        Content="إغلاق"
                        Style="{StaticResource SecondaryButton}"
                        Click="CloseButton_Click"
                        Height="56"
                        FontSize="18"/>
            </Grid>
        </Border>
    </Grid>
</Window>
